_schema-version: 3.3.0
ID: timesheet
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: timesheet-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: timesheet-auth
      - name: timesheet-db
      - name: timesheet-destination
  - name: timesheet
    type: approuter.nodejs
    path: app/router
    parameters:
      keep-existing-routes: true
      disk-quota: 256M
      memory: 256M
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true
      - name: timesheet-auth
      - name: timesheet-destination
      - name: timesheet-html5-runtime
    provides:
      - name: app-api
        properties:
          app-protocol: ${protocol}
          app-uri: ${default-uri}

  - name: timesheet-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: timesheet-db

  - name: timesheet-app-deployer
    type: com.sap.application.content
    path: gen
    build-parameters:
      build-result: app/
      requires:
        - name: timesheetemployeetimesheet
          artifacts:
            - employeetimesheet.zip
          target-path: app/
        - name: timesheetmnagerdashboard
          artifacts:
            - mnagerdashboard.zip
          target-path: app/
    requires:
      - name: timesheet-auth
      - name: srv-api
      - name: timesheet-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true
    parameters:
      config:
        destinations:
          - Name: srv-api
            URL: ~{srv-api/srv-url}
            Authentication: NoAuthentication
            Type: HTTP
            ProxyType: Internet
            HTML5.ForwardAuthToken: true
            HTML5.DynamicDestination: true

  - name: timesheetemployeetimesheet
    type: html5
    path: app/employeetimesheet
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []
  - name: timesheetmnagerdashboard
    type: html5
    path: app/mnagerdashboard
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: timesheet-destinations
    type: com.sap.application.content
    requires:
      - name: timesheet-auth
        parameters:
          service-key:
            name: timesheet-auth-key
      - name: timesheet-html5-repo-host
        parameters:
          service-key:
            name: timesheet-html5-repo-host-key
      - name: srv-api
      - name: timesheet-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: timesheet-html5-repository
              ServiceInstanceName: timesheet-html5-repo-host
              ServiceKeyName: timesheet-html5-repo-host-key
              sap.cloud.service: timesheet.service
            - Name: timesheet-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: timesheet-auth
              ServiceKeyName: timesheet-auth-key
              sap.cloud.service: timesheet.service

resources:
  - name: timesheet-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: timesheet-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
          redirect-uris:
            - https://*~{app-api/app-uri}/**
        role-collections:
          - name: 'Manager (timesheet ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Manager'
          - name: 'Employee (timesheet ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Employee'
    requires:
      - name: app-api
  - name: timesheet-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: timesheet-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
    requires:
      - name: srv-api

  - name: timesheet-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: timesheet-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
